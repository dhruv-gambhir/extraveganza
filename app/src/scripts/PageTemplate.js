import { Component } from "react";

export default class PageTemplate extends Component {
    render() {
        return (
            <div>
                Page couldn't be load
            </div>);
    }
}